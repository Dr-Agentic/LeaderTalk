# Session and cookie configuration
# Set to 'production' in production environments to enable secure cookies
NODE_ENV=production
# A strong random string for session encryption
SESSION_SECRET=your-strong-random-secret-key
# Domain for cookies - MUST match your actual domain exactly as it appears in the URL
# Example for Replit: your-replit-app.replit.app
# Example for custom domain: app.leadertalkcoach.com
COOKIE_DOMAIN=your-domain-name-here

# OpenAI API
OPENAI_API_KEY=your_openai_api_key

# Firebase Authentication (from Firebase console)
VITE_FIREBASE_API_KEY=your_firebase_api_key
VITE_FIREBASE_APP_ID=your_firebase_app_id
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id

# When using this example file:
# 1. Rename to '.env' or add these values to your Replit Secrets
# 2. Replace all placeholder values with your actual credentials
# 3. Make sure COOKIE_DOMAIN matches exactly what's shown in your browser address bar